apiVersion: apps/v1
kind: Deployment
metadata:
  name: rdv-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rdv-mongo
  template:
    metadata:
      labels:
        app: rdv-mongo
    spec:
      containers:
        - name: rdv-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: rdv-mongo-srv
spec:
  selector:
    app: rdv-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
